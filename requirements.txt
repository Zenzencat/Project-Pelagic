fastapi>=0.100.0
uvicorn>=0.22.0
requests>=2.30.0
numpy>=1.24.0
torch>=2.0.0
torchvision>=0.15.0
opencv-python>=4.7.0
pydantic>=2.0

# Optional: only needed to actually run src/analysis/era5_wind_check.py.
# Requires a free CDS account + API key (https://cds.climate.copernicus.eu) --
# not configured in this environment. See that module's docstring.
cdsapi>=0.7.0
xarray>=2023.1.0
netCDF4>=1.6.0
